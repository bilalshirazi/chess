services:
  - type: web
    name: chess-openings-academy
    runtime: python
    buildCommand: pip install uv && uv sync --no-dev
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
